# 回归清单（REGRESSION）

> 每次合并到 `dev_cbu` 或发布前，至少过一遍。

## 启动

- [ ] `bun run tauri dev` 正常打开，不卡 “Loading...”
- [ ] `bunx tsc --noEmit` 无错误

## 核心流程

- [ ] 新建任务（日 / 周 / 月视图入口）→ 出现在正确分组
- [ ] 勾选完成 / 取消完成 → 在 `tasks` 与 `completedTasks` 间正确迁移
- [ ] 编辑任务改状态 → 「完成记录」归组正确（**BUG-001 关注点**）
- [ ] 删除项目 → 其分类消失、无幽灵项（**BUG-002 关注点**）
- [ ] 删除分类 → 选中态复位（**BUG-003 关注点**）
- [ ] 项目 / 分类筛选在四视图联动正确

## 边界

- [ ] 无截止日期任务正常显示
- [ ] 跨月 / 跨年导航
- [ ] 语言切换 zh / en 文案无缺失
- [ ] 快速双击勾选框不产生重复 / 竞态
